# Deployment Notes

Deploy the RC-1.2.0 static site through GitHub Pages after confirming the custom domain and HTTPS certificate.

Verify:
- index.html loads
- assets resolve
- scripts execute
- responsive layouts display correctly
- search discovery files permit crawling and reference the live domain
- social preview image and application icons load
