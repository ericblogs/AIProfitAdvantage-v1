/**
 * Release metadata and validation rules for RC-1.2.0.
 * Public values only. No secrets are permitted.
 */
export const RELEASE_CONFIG = Object.freeze({
  release: 'RC-1.2.0',
  milestoneNumber: 2,
  milestone: 'Milestone 2 - Business Infrastructure Expansion',
  baseline: 'RC-1.1.0',
  platform: 'GitHub Pages',
  scope: 'Business Infrastructure Only'
});
