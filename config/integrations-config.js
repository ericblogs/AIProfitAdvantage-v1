/**
 * Integration readiness configuration.
 * Providers remain disabled until approved implementation phases.
 */
export const INTEGRATIONS_CONFIG = Object.freeze({
  crm: false,
  payments: false,
  analytics: false,
  authentication: false
});
